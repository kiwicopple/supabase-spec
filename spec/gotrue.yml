openref: 0.1

info:
  title:  GoTrue Client
  description: |
    Some awesome description.
  version: 
    0.0.1
  definition: '@supabase/gotrue-js'
  libraries: 
    - name: 'JavaScript'
      id: 'js'
      version: '0.0.1'
    - name: 'Python'
      id: 'py'
      version: '0.0.1'
  docs:
    sidebar:
      - name: 'Getting started'
        items:
          - index
          - Installing
          - createClient()
      - name: 'General functions'
        items:
          - signup()
          - login()
          - forgotPassword()
          - jwt()
      - name: 'User functions'
        items:
          - user()
          - user().data()
          - user().logout()
      - name: 'Hooks/callbacks'
        items:
          - onAuthStateChange()

pages:

  Installing: 
    description: |
     Some awesome description.

  createClient(): 
    description: |
     Some awesome description.

  signup():
    $ref: signup
    notes: |
      - A user can sign up either via email or OAuth.
      - If you provide `email`, `password` is also required
      - If you provide `access_token`, `password` is also required
    examples:
      - name: Sign up a new user
        js: |
          ```js
          const { error, data } = await gotrue.signup({
            email: 'example@email.com',
            password: 'example-password',
          })
          ```
        py: | 
          ```py
          # Not yet implemented
          ```

  login():
    $ref: login
    examples:
      - name: Log in a new user
        isSpotlight: true
        js: |
          ```js
          const { error, data } = await gotrue.signup({
            email: 'example@email.com',
            password: 'example-password',
          })
          ```
        py: | 
          ```py
          # Not yet implemented
          ```
      - name: Log in a new user 2
        js: |
          ```js
          const { error, data } = await gotrue.signup({
            email: 'example@email.com',
            password: 'example-password',
          })
          ```
        py: | 
          ```py
          # Not yet implemented
          ```
   
  forgotPassword():
    $ref: forgotPassword

  jwt():
    $ref: jwt

  user():
    $ref: user
    examples:
      - name: Sign up a new user
        js: |
          ```js
          const { error, data } = await signup({
            email: 'example@email.com',
            password: 'example-password',
          })
          ```

  user().data():
    $ref: LoggedInUser.data
    examples:
      - name: Sign up a new user
        js: |
          ```js
          const { error, user } = await gotrue.user()
          const { error, data } = await signup({
            email: 'example@email.com',
            password: 'example-password',
          })
          ```

  user().logout():
    $ref: LoggedInUser.logout
    examples:
      - name: Sign up a new user
        js: |
          ```js
          const { error, user } = await gotrue.user()
          const { error, data } = await signup({
            email: 'example@email.com',
            password: 'example-password',
          })
          ```

  onAuthStateChange(): 
    $ref: onAuthStateChange

    